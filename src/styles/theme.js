import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#EDE7E3",
      dark: "#002884",
    },
    secondary: {
      main: "#FFFFF",
      dark: "#ba000d",
    },
  },
});
export default theme;